# fail2ban jail for ergo-proxy-node
# Installed automatically by the .deb package when fail2ban is present.

[ergo-proxy]
enabled = true
filter = ergo-proxy
logpath = /var/log/ergo-proxy/ergo-proxy.log
# 5 violations within 10 minutes = 1 hour ban
maxretry = 5
findtime = 600
bantime = 3600
